ヒント1: cronログの全体像を把握せよ
======================================

ログが大量にある。まずはどんなジョブが実行されているか集計してみよう。

  cat mystery/logs/cron.log | grep CMD | cut -d'(' -f3 | cut -d')' -f1 | sort | uniq -c | sort -rn

普段実行されていないコマンドはないか？
